# Test Java Sources

This tree contains Java-based automated tests.

- Mirror the production package layout where practical.
- Use slice tests or focused unit tests when a full `@SpringBootTest` is not necessary.
- Keep package bridges lightweight and store real tests deeper in the hierarchy.
