# Main Java Sources

This tree contains the application's production Java packages.

- Follow the existing package layout rooted at `com.data.service.core`.
- Prefer placing real behavior in the deepest appropriate package instead of these package-path bridge folders.
- Keep package declarations and directory structure aligned.
