# Package Bridge

This folder is part of the mirrored Java test package path.

- Avoid placing substantive tests here.
- Keep package declarations aligned with the directory tree.
- Put real tests under the matching `com.data.service.core` subpackages.
