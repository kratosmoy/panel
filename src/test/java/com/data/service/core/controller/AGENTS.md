# Controller Tests

This package verifies controller-level HTTP behavior.

- Focus on routes, status codes, payload shape, and controller-service interaction boundaries.
- Keep controller tests aligned with the dynamic entity API contracts.
- Add regression coverage when endpoint semantics or error handling change.
