# Search Tests

This package verifies query parsing, criteria, and specification behavior.

- Keep operator coverage explicit whenever search semantics expand.
- Prefer focused tests that isolate one search rule at a time.
- Update these tests together with `search/` and metric-related service changes.
