# Repositories

This package contains Spring Data repository interfaces.

- Keep repositories focused on persistence access patterns.
- Favor derived queries or explicit repository methods over leaking query details into controllers.
- If repository structure changes broadly, review the generator inputs and templates first.
