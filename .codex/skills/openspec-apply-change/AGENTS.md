# OpenSpec Apply Skill

This skill is for implementing an approved OpenSpec change.

- Keep it implementation-oriented: read tasks first, then execute, then verify.
- Preserve references to OpenSpec artifacts and do not skip validation steps.
- If the apply flow changes, update the matching Windsurf skill and workflow text too.
