# Test Resources

This folder contains test-only configuration.

- Keep test startup deterministic and self-contained.
- Prefer minimal overrides that clearly explain why test behavior differs from local or prod behavior.
- Update these files whenever security or profile defaults change in main resources.
