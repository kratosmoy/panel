# OpenSpec Workspace

This directory stores spec-driven change management artifacts for the repository.

- Keep `config.yaml`, change proposals, designs, tasks, and capability specs internally consistent.
- Prefer additive change folders under `openspec/changes/<change-name>/`.
- When implementation diverges from a spec, update the spec rather than leaving stale intent behind.
