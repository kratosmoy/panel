# Security Docs

This directory holds security-focused operational notes.

- Keep endpoint names, profile flags, and authentication flows consistent with `src/main/java/com/data/service/core/security`.
- Document real deployment constraints, especially around mTLS and session handling.
- Update this doc set whenever security filter chains or auth entrypoints change.
