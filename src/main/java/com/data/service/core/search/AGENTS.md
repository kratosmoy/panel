# Search Support

This package contains reusable query and metric request infrastructure.

- Keep request DTOs simple and serialization-friendly.
- Changes to search operators or predicate construction should be backed by focused tests.
- Preserve compatibility between request objects, `GenericSpecification`, and `GenericService`.
