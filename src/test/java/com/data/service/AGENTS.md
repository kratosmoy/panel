# Package Bridge

This directory continues the mirrored test namespace.

- Keep it lightweight and avoid dropping feature tests at this level.
- Let the `core` package and its children carry the actual test coverage.
- Maintain consistent package declarations.
