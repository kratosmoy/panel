# Gradle Build Logic

This folder contains shared Gradle logic and code generation support.

- Keep build scripts compatible with the wrapper version committed in this repo.
- Prefer deterministic tasks with clear inputs and outputs.
- Changes here can affect code generation and compilation for the whole project, so verify with Gradle after edits.
