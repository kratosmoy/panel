# Module Authorization Context Spec

This capability describes the normalized backend user context returned to the frontend.

- Keep claim, group, entitlement, permission, and data-scope semantics explicit.
- Align this spec with `BackendUserContext`, its mapper, and the `/api/me` response contract.
- Update the spec if authorization vocabulary or payload structure changes.
