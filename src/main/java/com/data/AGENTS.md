# Package Bridge

This folder is a namespace step on the way to the real application packages.

- Avoid adding implementation classes here unless the package design changes intentionally.
- Keep package names and directories synchronized.
- Favor deeper `service/core` packages for actual behavior.
