# Package Bridge

This directory is a namespace step for mirrored tests.

- Keep it as a bridge folder, not a feature boundary.
- Preserve package-name and directory alignment.
- Add new tests deeper alongside the relevant production package.
