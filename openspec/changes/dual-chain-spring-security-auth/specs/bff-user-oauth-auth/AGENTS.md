# BFF User OAuth Spec

This capability documents browser-user authentication through PingFederate.

- Keep the spec focused on session-backed user auth and the frontend contract endpoints.
- Reflect the current behavior of `/api/auth/login`, `/api/auth/logout`, and `/api/me`.
- Update it whenever claim mapping, redirect flow, or session behavior changes.
