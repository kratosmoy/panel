# Service Tests

This package verifies service-layer query, persistence, and metric behavior.

- Keep service tests focused on business behavior rather than controller concerns.
- Update metric assertions whenever aggregation logic changes.
- Coordinate with search tests when filter semantics are involved.
