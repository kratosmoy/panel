# Core Tests

This package contains integration and package-level tests for the core application.

- Keep cross-cutting integration tests here when they span multiple subpackages.
- Name tests after the class or capability they verify.
- When production contracts change, update the nearest test package rather than adding unrelated coverage here.
