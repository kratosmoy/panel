# Package Bridge

This folder exists to hold the Java package path.

- Do not place feature code here unless the package boundary truly belongs here.
- Keep the directory structure and `package` statements aligned.
- Put application behavior under deeper `com.data.service.core` packages.
