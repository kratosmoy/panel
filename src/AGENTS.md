# Source Tree

Application code and tests live under this directory.

- Keep production changes under `src/main` and verification changes under `src/test`.
- Maintain parity between behavior changes and the tests that prove them.
- Configuration, schema, and generated-model inputs live under `src/main/resources`.
